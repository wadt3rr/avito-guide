# Образ для разработки виджета: пересборка на лету плюс раздача готового файла.
# Зависимости ставятся при старте контейнера, чтобы node_modules оказались
# в смонтированной папке и редактор видел типы.
FROM node:22-alpine

WORKDIR /app

EXPOSE 8082

CMD ["sh", "-c", "npm install && npm run dev"]
